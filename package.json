{
  "name": "is-iso-date",
  "description": "A simple utility to check if a string is an ISO-compliant date.",
  "author": "Andy Burke <aburke@bitflood.org>",
  "license": "MIT",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/arn4v/is-iso-date.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts --dts",
    "test": "vitest"
  },
  "keywords": [
    "date",
    "match",
    "ISO",
    "string",
    "regexp",
    "regular expression"
  ],
  "devDependencies": {
    "tsup": "^6.2.3",
    "typescript": "^4.8.4",
    "vitest": "^0.24.0"
  }
}
